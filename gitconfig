[user]
	name = Matt Heindel
  email = mheindel94@gmail.com
[core]
  editor = vim
  autocrlf = input
  safecrlf = true
[alias]
  co = checkout
  ci = commit
  st = status
  br = branch
  hist = log --pretty=format:'%h %ad | %s%d [%an]' --graph --date=short
  type = cat-file -t
  dump = cat-file -p
  myhist = log --pretty=format:'%h %ad | %s%d [%an]' --graph --date=short --author='Matt Heindel'

[credential]
	helper = store
